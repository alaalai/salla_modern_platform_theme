# ثيم المنصة العصرية - دليل المطور الكامل

## 📋 مقدمة

ثيم المنصة العصرية هو ثيم احترافي وحديث لمنصة سلة، مصمم خصيصاً للمتاجر المتخصصة في المنتجات الرقمية والخدمات الإبداعية. يتوافق بالكامل مع معايير سلة وتوثيقها.

**الإصدار:** 1.0.0  
**المؤلف:** Salla Theme Developer  
**الترخيص:** MIT  
**الدعم:** https://github.com/alaalai/salla_modern_platform_theme/issues

---

## 🎯 المميزات الرئيسية

- ✅ **تصميم حديث واحترافي** - نظام ألوان متناسق وطباعة عصرية
- ✅ **تصميم مستجيب** - يعمل بسلاسة على جميع الأجهزة
- ✅ **مكونات تفاعلية** - رأس، تذييل، بطاقات منتجات، شهادات
- ✅ **دعم متعدد اللغات** - العربية والإنجليزية
- ✅ **أداء عالي** - محسّن للسرعة والـ SEO
- ✅ **سهولة التخصيص** - إعدادات شاملة للتجار
- ✅ **توافق Salla كامل** - يدعم جميع APIs والميزات

---

## 📁 بنية المجلدات

```
salla_modern_platform_theme/
├── views/                          # ملفات Twig Templates
│   ├── layouts/
│   │   └── master.twig            # المخطط الرئيسي
│   ├── pages/
│   │   ├── index.twig             # الصفحة الرئيسية
│   │   ├── product/
│   │   │   └── index.twig         # صفحة تفاصيل المنتج
│   │   ├── cart.twig              # سلة التسوق
│   │   ├── checkout.twig          # صفحة الدفع
│   │   ├── blog/
│   │   ├── customer/
│   │   └── orders/
│   └── components/
│       ├── header/index.twig      # مكون الرأس
│       ├── footer/index.twig      # مكون التذييل
│       ├── product/
│       │   └── card.twig          # بطاقة المنتج
│       └── home/                   # مكونات الصفحة الرئيسية
├── assets/                         # ملفات الأصول
│   ├── styles/
│   │   └── main.css               # الأنماط الرئيسية
│   ├── js/
│   │   └── theme.js               # سكريبت الثيم
│   └── images/                    # الصور
├── locales/                       # ملفات اللغات
│   ├── ar.json                    # الترجمات العربية
│   └── en.json                    # الترجمات الإنجليزية
├── twilight.json                  # ملف إعدادات الثيم
└── README.md                      # هذا الملف
```

---

## ⚙️ الإعدادات المتاحة

يمكن للتاجر تخصيص الثيم من خلال الإعدادات التالية في لوحة Salla Partners:

### 1. الألوان (Colors)
- **اللون الأساسي** (`primary_color`) - الافتراضي: `#0F172A`
- **لون التمييز** (`accent_color`) - الافتراضي: `#84CC16`
- **لون الخلفية** (`background_color`) - الافتراضي: `#FFFFFF`
- **اللون الثانوي** (`secondary_color`) - الافتراضي: `#F3F4F6`

### 2. الأعدادات العامة
- **شريط الإعلانات** - عرض/إخفاء شريط الإعلانات العلوي
- **نص الإعلان** - تخصيص رسالة الإعلان
- **النشرة البريدية** - تفعيل/تعطيل الاشتراك
- **شهادات العملاء** - عرض/إخفاء قسم الشهادات
- **قائمة الرغبات** - تفعيل/تعطيل المفضلة

### 3. المكونات المتقدمة

#### Hero Banner (شريط البطل)
```twig
{
  "title": "العنوان الرئيسي",
  "subtitle": "العنوان الفرعي",
  "background_image": "رابط الصورة",
  "cta_text": "نص الزر",
  "cta_link": "/products"
}
```

#### Product Showcase (عرض المنتجات)
```twig
{
  "title": "عنوان القسم",
  "description": "وصف القسم",
  "product_limit": 6
}
```

#### Features Section (قسم الميزات)
```twig
{
  "features": [
    {
      "feature_icon": "رابط الأيقونة",
      "feature_title": "عنوان الميزة",
      "feature_description": "وصف الميزة"
    }
  ]
}
```

---

## 🎨 نظام التصميم

### الألوان الأساسية
| الاسم | الكود | الاستخدام |
|------|------|----------|
| Primary | `#0F172A` | الرأس، التذييل، العناصر الرئيسية |
| Accent | `#84CC16` | أزرار الدعوة، التمييز |
| Background | `#FFFFFF` | خلفية المحتوى |
| Secondary | `#F3F4F6` | خلفيات فرعية |

### الطباعة
- **العناوين:** Poppins (Bold: 700, ExtraBold: 800)
- **النصوص:** Inter (Normal: 400, Medium: 500)
- **أحجام:** 12px, 14px, 16px, 18px, 20px, 24px, 32px, 48px

### المسافات (8px base)
- `xs`: 4px
- `sm`: 8px
- `md`: 16px
- `lg`: 24px
- `xl`: 32px
- `2xl`: 48px
- `3xl`: 64px

### الزوايا المستديرة
- `sm`: 4px
- `md`: 8px
- `lg`: 12px
- `xl`: 16px

### الظلال
- `sm`: `0 1px 2px rgba(0, 0, 0, 0.05)`
- `md`: `0 4px 6px -1px rgba(0, 0, 0, 0.1)`
- `lg`: `0 10px 15px -3px rgba(0, 0, 0, 0.1)`
- `xl`: `0 20px 25px -5px rgba(0, 0, 0, 0.1)`

---

## 🔧 كيفية التخصيص

### 1. تغيير الألوان

عدّل ملف `twilight.json`:

```json
{
  "settings": [
    {
      "type": "color",
      "id": "primary_color",
      "label": "اللون الأساسي",
      "value": "#0F172A"
    }
  ]
}
```

أو عدّل ملف `assets/styles/main.css`:

```css
:root {
  --primary-color: #0F172A;
  --accent-color: #84CC16;
}
```

### 2. إضافة صفحة جديدة

1. أنشئ ملف Twig جديد في `views/pages/`:

```twig
{% extends "layouts/master" %}

{% block content %}
<div class="container">
  <h1>عنوان الصفحة</h1>
  <!-- محتوى الصفحة -->
</div>
{% endblock %}
```

2. قم بتسجيل الصفحة في Salla CMS

### 3. إضافة مكون مخصص

1. أنشئ مكون جديد في `views/components/`:

```twig
{# Custom Component #}
<div class="custom-component">
  <!-- محتوى المكون -->
</div>
```

2. سجل المكون في `twilight.json`:

```json
{
  "components": [
    {
      "name": "custom-slider",
      "title": "مكون مخصص",
      "path": "home.custom-slider",
      "fields": [...]
    }
  ]
}
```

### 4. تعديل الرأس والتذييل

عدّل ملفات:
- `views/components/header/index.twig`
- `views/components/footer/index.twig`

---

## 🌐 الدعم متعدد اللغات

الثيم يدعم العربية والإنجليزية تلقائياً.

### استخدام الترجمات في Templates

```twig
{# الطريقة الأساسية #}
{{ 'common.welcome' | t }}

{# مع معاملات إضافية #}
{{ 'products.title' | t }}

{# القيمة الافتراضية #}
{{ 'custom_key' | t('Value default') }}
```

### إضافة ترجمات جديدة

1. عدّل `locales/ar.json`:

```json
{
  "custom": {
    "key": "القيمة العربية"
  }
}
```

2. عدّل `locales/en.json`:

```json
{
  "custom": {
    "key": "English value"
  }
}
```

3. استخدمها في Twig:

```twig
{{ 'custom.key' | t }}
```

---

## 📱 الاستجابة (Responsive Design)

الثيم يتبع نهج mobile-first:

| جهاز | العرض | نقطة الكسر |
|------|------|-----------|
| Mobile | < 640px | sm |
| Tablet | 640px - 1024px | md, lg |
| Desktop | > 1024px | xl, 2xl |

### Media Queries

```css
/* Mobile First */
.example { /* أنماط الهاتف */ }

/* Tablets */
@media (min-width: 768px) {
  .example { /* أنماط الأجهزة اللوحية */ }
}

/* Desktop */
@media (min-width: 1024px) {
  .example { /* أنماط سطح المكتب */ }
}
```

---

## ♿ سهولة الوصول (Accessibility)

الثيم يتوافق مع معايير WCAG 2.1:

- ✅ نسبة تباين ≥ 4.5:1
- ✅ حجم الخط ≥ 16px
- ✅ حجم الأزرار ≥ 44x44px
- ✅ دعم لوحة المفاتيح
- ✅ ملصقات مرتبطة بالحقول
- ✅ ARIA labels
- ✅ Skip links

### أمثلة الاستخدام

```twig
{# Skip Link #}
<a href="#main-content" class="sr-only">اذهب إلى المحتوى الرئيسي</a>

{# ARIA Labels #}
<button aria-label="إغلاق">X</button>

{# Focus Styles #}
<a href="#" tabindex="0">رابط</a>

{# Semantic HTML #}
<nav role="navigation" aria-label="الملاحة الرئيسية">
  <ul role="menubar">
    <li role="menuitem"><a href="#">عنصر القائمة</a></li>
  </ul>
</nav>
```

---

## 🔌 التكامل مع Salla SDK

الثيم يتضمن دعماً كاملاً لـ Salla SDK:

```javascript
// تحقق من توفر SDK
if (window.salla) {
  // جاهزية SDK
  window.salla.onReady(() => {
    // تطبيق الكود
  });

  // إضافة إلى السلة
  window.salla.cart.addItem(productId, quantity);

  // المفضلة
  window.salla.wishlist.toggle(productId);

  // بيانات العميل
  window.salla.customer.get();

  // تحديد اللغة والاتجاه
  const locale = window.salla.lang();
  const direction = window.salla.dir(); // 'ltr' أو 'rtl'
}
```

---

## 🚀 الأداء والتحسينات

### تحسينات مُطبقة
- ✅ Lazy loading للصور
- ✅ تقليل حجم CSS/JS
- ✅ استخدام CSS Grid و Flexbox
- ✅ Minification للملفات
- ✅ Caching الصور
- ✅ تحسينات SEO

### نصائح إضافية
1. استخدم أدوات مثل Lighthouse
2. اضغط الصور قبل الرفع
3. استخدم CDN للملفات الثقيلة
4. اختبر على أجهزة مختلفة

---

## 🧪 الاختبار

### الاختبار اليدوي
```bash
# على مختلف الأجهزة والمتصفحات
- Desktop: Chrome, Firefox, Safari, Edge
- Mobile: Chrome, Safari, Firefox
- Tablet: iOS Safari, Chrome
```

### أدوات الاختبار
- Lighthouse (Google)
- WAVE (Accessibility)
- Mobile-Friendly Test
- PageSpeed Insights

---

## 📦 النشر على App Store

قبل النشر، تأكد من:

1. ✅ **Industry Focus** - حدد الصناعة المستهدفة
2. ✅ **Uniqueness** - تأكد من أن الثيم فريد وليس نسخة من Theme Raed
3. ✅ **Components** - أضف مكونات مخصصة قابلة للتخصيص
4. ✅ **File Size** - أقل من 1MB (public) أو 2MB (private)
5. ✅ **Screenshots** - أضف صور معاينة عالية الجودة
6. ✅ **Documentation** - وثق المميزات والإعدادات

---

## 🐛 استكشاف الأخطاء

### الثيم لا يظهر
- تحقق من صحة `twilight.json`
- تأكد من توفر جميع الملفات
- افحص لوحة Salla Partners

### الأنماط لا تعمل
- تحقق من ملف `assets/styles/main.css`
- امسح الـ cache
- استخدم DevTools للتحقق

### الترجمات لا تظهر
- تأكد من صحة ملفات `locales/`
- تحقق من مفاتيح الترجمة
- استخدم الفلترة الصحيحة `| t`

---

## 📚 الموارد الإضافية

- [Salla Documentation](https://docs.salla.dev/)
- [Twilight Engine](https://docs.salla.dev/doc-421878)
- [Salla SDK](https://docs.salla.dev/doc-1234)
- [Twig Template Engine](https://twig.symfony.com/)

---

## 📞 الدعم والمساعدة

للمساعدة والدعم:
- 📧 البريد الإلكتروني: support@salla.dev
- 💬 مجتمع المطورين: https://t.me/salladev
- 🐙 GitHub Issues: https://github.com/alaalai/salla_modern_platform_theme/issues

---

**آخر تحديث:** 26 نوفمبر 2025  
**الإصدار:** 1.0.0
